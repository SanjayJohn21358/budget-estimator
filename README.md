# budget-estimator
Tool to help estimate budgets using Streamlit and user-fed Google Sheets for easy integration

## Run tests

```bash
make test
```

If you prefer running directly:

```bash
python3 -m unittest discover -s tests -v
```
